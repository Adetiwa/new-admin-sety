import {
  axios_default
} from "./chunk-OFXDZ6V2.js";
import "./chunk-DC5AMYBS.js";

// node_modules/axios-auth-refresh/dist/index.esm.js
var r = { statusCodes: [401], deduplicateRefresh: true, maxRetries: 3 };
function t(e, r2, t2, s2) {
  var n2, o;
  return !!e && (!(null === (n2 = e.config) || void 0 === n2 ? void 0 : n2.skipAuthRefresh) && (!!(r2.interceptNetworkError && !e.response && 0 === e.request.status || e.response && ((null == r2 ? void 0 : r2.shouldRefresh) ? function(e2, r3) {
    try {
      return e2(r3);
    } catch (e3) {
      return console.error("axios-auth-refresh: shouldRefresh callback threw an error:", e3), false;
    }
  }(r2.shouldRefresh, e) : null === (o = r2.statusCodes) || void 0 === o ? void 0 : o.includes(e.response.status))) && (e.response || (e.response = { config: e.config }), !r2.deduplicateRefresh || !s2.skipInstances.includes(t2))));
}
function s(s2, n2, o = {}) {
  if ("function" != typeof n2) throw new Error("axios-auth-refresh requires `refreshAuthCall` to be a function that returns a promise.");
  const u = function(e, r2) {
    return Object.assign(Object.assign({}, e), r2);
  }(r, o), i = { skipInstances: [], refreshCall: void 0, requestQueueInterceptorId: void 0 };
  return s2.interceptors.response.use((e) => e, (r2) => {
    if (!t(r2, u, s2, i)) return Promise.reject(r2);
    const o2 = r2.config;
    if (o2 && (o2.__authRefreshRetryCount = (o2.__authRefreshRetryCount || 0) + 1, o2.__authRefreshRetryCount > u.maxRetries)) return Promise.reject(r2);
    u.deduplicateRefresh && !i.skipInstances.includes(s2) && i.skipInstances.push(s2);
    const c = function(e, r3, t2) {
      return t2.refreshCall || (t2.refreshCall = r3(e), "function" == typeof t2.refreshCall.then) ? t2.refreshCall : (console.warn("axios-auth-refresh requires `refreshTokenCall` to return a promise."), Promise.reject());
    }(r2, n2, i);
    return function(r3, t2, s3) {
      void 0 === t2.requestQueueInterceptorId && (t2.requestQueueInterceptorId = r3.interceptors.request.use((r4) => t2.refreshCall.catch(() => {
        throw new axios_default.CanceledError("Request call failed");
      }).then(() => {
        if (s3.onRetry) try {
          return s3.onRetry(r4);
        } catch (e) {
          return console.error("axios-auth-refresh: onRetry callback threw an error:", e), r4;
        }
        return r4;
      }))), t2.requestQueueInterceptorId;
    }(s2, i, u), c.catch((e) => Promise.reject(e)).then(() => function(e, r3) {
      return e.config.skipAuthRefresh = true, r3(e.config);
    }(r2, function(e, r3) {
      return r3.retryInstance || e;
    }(s2, u))).finally(() => function(e, r3) {
      e.interceptors.request.eject(r3.requestQueueInterceptorId), r3.requestQueueInterceptorId = void 0, r3.refreshCall = void 0, r3.skipInstances = r3.skipInstances.filter((r4) => r4 !== e);
    }(s2, i));
  });
}
var n = s;
export {
  s as createAuthRefresh,
  n as createAuthRefreshInterceptor,
  s as default
};
//# sourceMappingURL=axios-auth-refresh.js.map
